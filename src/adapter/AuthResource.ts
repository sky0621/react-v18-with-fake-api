import { LoginPayload } from '../domain/model/auth';

export const login = (loginPayload: LoginPayload) => {
  //
};

export const logout = (token: string) => {
  //
};

export const isLogin = () => {
  //
};
